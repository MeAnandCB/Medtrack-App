import 'package:flutter/material.dart';

class BookingConfrimation extends StatelessWidget {
  const BookingConfrimation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
